#include<stdio.h>
#include<math.h>
int main(){

	int t, a, b;
	scanf("%d", &t);
	while(t--){
		scnaf("%d %d", &a, &b);
		int count=0;
		int i=a;
		for(i; i<=b; i++){
			if(a>1 && b>1 && a)
		}
	}

    return 0;

}
